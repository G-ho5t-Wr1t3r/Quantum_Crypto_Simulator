node_modules
dist
*.local

# TypeScript's incremental build cache. A local artifact with a timestamp in it:
# committed, it would produce a diff on every machine that ran a build.
tsconfig.tsbuildinfo
